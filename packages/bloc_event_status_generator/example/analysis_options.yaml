include: package:very_good_analysis/analysis_options.yaml

linter:
  rules:
    # This is an example app, so we don't need to document public members
    public_member_api_docs: false
    # This is an example app and we use print statements for demonstration.
    avoid_print: false
